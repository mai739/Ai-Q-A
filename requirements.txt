# FastAPI และ Uvicorn สำหรับรัน API
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# LINE Messaging API SDK
line-bot-sdk>=2.4.2

# LangChain และส่วนประกอบที่ใช้
langchain>=0.2.0
langchain-community>=0.2.0
langchain-chroma>=0.1.0
langchain-ollama>=0.1.0

# สำหรับ Embeddings และ Chat กับ Ollama
# (Ollama ต้องติดตั้งแยกในเครื่อง)

# ตัวโหลดไฟล์เอกสาร
pypdf>=4.2.0   # ใช้กับ PyPDFLoader
python-multipart>=0.0.9  # เผื่อรองรับการอัปโหลดไฟล์

# จัดการ Environment Variables
python-dotenv>=1.0.1

# อื่นๆ ที่ใช้บ่อยกับ LangChain
chromadb>=0.5.0